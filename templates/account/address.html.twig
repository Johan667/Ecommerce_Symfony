{% extends 'base.html.twig' %}

{% block title %}RaveShop - Mes adresses{% endblock %}

{% block content %}
<h1>Mes adresses</h1>
<a href="{{ path('app_account_address_add')}}" class="btn btn-info btn-sm float-right">Ajouter une adresse</a>
Ici vous pouvez modifier toutes les informations relatives à vos adresses.<br>
<a href="{{ path('app_account_address')}}">Retour</a>
<hr>
{% if app.user.addresses| length == 0 %}
{#} Si dans l'objet user il n'y à pas d'adresse {#}
{#} app est une variable qui stocke une multitude d'informations sur les sessions les users ...{#}
<p class="text-center">
    Vous n'avez pas encore ajouté d'adresse dans votre compte. Pour en ajouté une, veuillez <a
        href="{{ path('app_account_address_add')}}">cliquer ici</a>.
</p>
{% else %}

<div class="row">
{% for address in app.user.addresses %}
    <div class="col-md-4">
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title text-center">{{ address.name }}</h5>
                <p class="card-text">{{ address.address }}<br>
                    {{ address.city }}<br>
                    {{ address.postal }}, {{ address.country }}<br>                
                </p>
                <a href="{{path('app_account_address_modify', {'id': address.id } )}}">Modifier</a> | 
                <a href="{{path('app_account_address_delete', {'id': address.id } )}}">Supprimer</a>
            </div>
        </div>
    </div>
{% endfor %}
</div>

{% endif %}
<div class="similary">
</div>
{% endblock %}